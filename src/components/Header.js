import React from 'react';

function Header(){
    return(
        <header class="page-header">
          <nav>
            <a class="logo">Home</a>
            <a class="logo">Client</a>
          </nav>
        </header>
    )
}

export default Header